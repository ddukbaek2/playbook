# STORYENGINE.md

상황극 엔진(`libs/playbook-engine.js`)이 매 턴 LLM 에 전달하는 시스템 프롬프트의 구성과, 그 안에 포함된 각 지침의 의도/규칙을 정리한 문서입니다. 엔진의 실제 텍스트는 `libs/playbook-engine.js/src/playbooksession.js` 의 메서드들이 생성하며, 이 문서와 코드는 수동으로 동기화합니다.

---

## 1. 시스템 프롬프트 구성

매 턴 LLM 에 전달되는 시스템 프롬프트는 `PlaybookSession.buildSystemPrompt()` 가 다음 순서로 섹션을 이어붙여 만듭니다. 섹션 사이는 빈 줄 한 줄(`\n\n`)로 구분됩니다.

| 순서 | 섹션 | 출처 | 동적/정적 |
| ---- | ---- | ---- | ---- |
| 1 | 페르소나 | `Persona.toPromptText()` | 정적 |
| 2 | 책 시스템 프롬프트 | `book.systemPrompt` (`assets/data/books.json`) | 정적 |
| 3 | 출력 포맷 지침 | `buildOutputFormatGuideText()` | 정적 |
| 4 | 사용자 입력 보존 지침 | `buildUserInputPreservationGuideText()` | 정적 |
| 5 | NPC 반응 인과 지침 | `buildNpcReactionGuideText()` | 정적 |
| 6 | 현재 진행 상황 / 전환 지침 | `buildEpisodeGuideText()` | 매 턴 동적 |

이 외에 LLM 에는 별도로 최근 `MAX_TURNS = 20` 턴의 메시지 히스토리(`buildLlmContents()`)가 함께 전달됩니다. 히스토리에는 `book.opening` 이 첫 user 엔트리로 보존되며, 그 외에는 최근 턴만 슬라이딩 윈도우로 잘립니다.

---

## 2. 출력 포맷 지침 (Output Format)

**역할**: LLM 응답을 채팅 버블에서 가독성 있게 렌더링하기 위한 단락 분리 규칙.

**규칙**:
- 장면 묘사/설명과 등장인물의 대사 사이에는 반드시 빈 줄(`\n\n`) 을 넣어 분리한다.
- 등장인물의 대사는 큰따옴표(`"..."`) 로 감싸고, 묘사 단락과는 별도의 단락에 배치한다.
- 서로 다른 등장인물의 대사가 연속될 때에도 각 대사 사이에 빈 줄을 넣어 단락을 분리한다.

**예시**:
```
어쩌고 저쨌다.

"대사"

이어서 또 어쩌고 했다.
```

**왜 필요한가**: `src/app/documents/playdocument.js` 의 메시지 버블은 `white-space: pre-wrap` 으로 렌더링되므로 `\n\n` 이 시각적인 빈 줄이 됩니다. LLM 이 한 단락에 묘사와 대사를 몰아 쓰면 사용자에게 "벽 같은 텍스트(wall of text)" 가 보입니다. `book.opening` 도 같은 원칙으로 작성합니다.

---

## 3. 사용자 입력 보존 지침 (User Input Preservation) — 최우선 규칙

**역할**: 사용자가 입력한 대사/행동/상황을 LLM 이 임의로 변형하지 못하게 강제. 이 섹션은 다른 모든 지침보다 우선합니다.

**규칙**:
- 사용자가 입력한 대사, 행동, 상황 묘사는 **100% 그대로 발생한 사실**로 받아들인다.
- 사용자의 입력 내용을 절대로 임의로 요약, 축약, 생략, 대체, 각색, 부정, 무시하지 않는다.
- 사용자의 입력이 큰따옴표로 감싸진 대사라면 주인공이 그 문구를 **토씨 하나 틀리지 않고** 그대로 말한 것으로 처리한다.
- 사용자의 입력이 상황/행동 묘사라면 그 행동이 그대로 일어난 것으로 처리한다.
- 사용자의 입력이 현재 에피소드 가이드와 충돌하거나 시나리오 진행을 망가뜨리는 경우에도 **사용자의 입력을 우선**한다. 진행이 어색해지거나 결말이 바뀌더라도 사용자의 선택을 존중해 자연스러운 후속 반응만 묘사한다.
- 응답에서는 사용자의 입력 자체를 다시 반복해 출력하지 않는다. 그 입력에 대한 NPC/세계의 반응과 후속 묘사만 작성한다.

**왜 필요한가**: 모델이 시나리오 정합성을 유지하려고 사용자의 황당한 대사/행동을 "완화" 하거나 "재해석" 해 진행하는 경향이 있습니다. 이 프로젝트는 플레이어 행위성(player agency) 을 시나리오 정합성보다 위에 둡니다.

---

## 4. NPC 반응 인과 지침 (NPC Reaction Causality)

**역할**: NPC 의 모든 행동/대사 변화가 직전 사용자 입력과 인과적으로 연결되어 보이도록 강제.

**규칙**:
- 등장인물(NPC)의 행동, 표정, 대사가 직전 사용자 입력의 어떤 부분에 반응한 것인지 그 사유가 응답 본문에서 항상 드러나야 한다.
- 속마음(내적 독백) 묘사가 아니어도 된다. 표정 변화, 시선, 호흡, 몸짓, 어조, 말끝의 망설임, 짧은 되묻기, 주변 묘사 등 어떤 외적 단서로든 "왜 그렇게 반응했는지" 가 사용자 입력과 인과로 연결되어야 한다.
- 사용자의 대사/행동이 NPC 에게 황당하거나 예상 밖일수록, 그 당황/혼란/분노/웃음 등이 어떤 지점에서 촉발됐는지 더 분명하게 표현한다. (예: NPC 가 사용자의 말 중 특정 단어를 되뇌인다, 사용자가 한 행동의 결과물을 시선으로 좇는다)
- 이유 없이 NPC 가 갑자기 새로운 행동을 시작하거나 화제를 돌리지 않는다. 모든 반응 변화는 직전 사용자 입력 또는 직전 상황 묘사와 명시적/암시적 인과를 가진다.

**왜 필요한가**: 사용자가 의도적으로 비상식적인 대사/행동을 시도했을 때 NPC 의 반응 이유가 응답에 드러나지 않으면, 모델이 사용자의 입력을 "무시" 한 것처럼 느껴집니다. 사용자 입력 보존 지침이 사실 보존을 담당한다면, 이 지침은 그 사실에 대한 NPC 반응의 가독성을 담당합니다.

---

## 5. 현재 진행 상황 / 전환 지침 (Episode Guide & Transition)

**역할**: 책의 챕터/에피소드 구조 위에서 현재 어느 화에 있는지, 다음 화로의 전환을 언제/어떻게 표기할지 알려주는 매 턴 동적 섹션.

**구성**:
- `[현재 진행 상황]` 블록
  - 현재 챕터 제목
  - 현재 화 ID + 제목
  - 화 요약(summary)
  - 가능한 다음 화 목록 (`transitions` 배열)
- `[전환 지침]` 블록
  - **다음 화가 있는 경우**: 흐름이 자연스러운 종결에 이르면 응답의 마지막 줄에 `[다음화: <id>]` 형식으로 다음 화 ID 를 표기. 가능한 다음 화 ID 중에서 선택. 사용자의 행동을 충분히 반영한 흐름에서만 전환하고 강제로 빠르게 진행하지 않음.
  - **다음 화가 없는 경우 (마지막 화)**: `[다음화]` 태그를 출력하지 않고 결말까지 자연스럽게 전개.

**파싱**: `PlaybookSession.parseTransitionTag()` 가 정규식 `/\[다음화\s*:\s*([\w가-힣_-]+)\s*\]/` 으로 응답 끝에서 태그를 분리하고, 매칭된 화 ID 가 유효하면 `currentEpisodeId` 를 갱신하고 `visitedEpisodeIds` 에 추가합니다. 응답 텍스트에서는 태그가 제거된 채로 UI 에 표시됩니다.

---

## 6. 책 시스템 프롬프트 (`book.systemPrompt`) 작성 규칙

`assets/data/books.json` 의 각 책 항목에 들어가는 `systemPrompt` 는 그 책에만 적용되는 세계관/톤/주인공 식별 정보를 담습니다. 전역 지침(출력 포맷 / 사용자 입력 보존 / NPC 반응 인과) 은 엔진이 자동으로 추가하므로 책별 프롬프트에 중복으로 적어 둘 필요는 없습니다.

권장 항목:
- 게임 마스터 역할 명시 ("당신은 소설형 상황극의 게임 마스터입니다.")
- 답변 분량/형식 가이드 ("매 답변은 3~6문장의 장면 묘사 + 등장인물의 행동/대사로 구성합니다.")
- 주인공이 누구인지, 사용자 입력을 누구의 행동/대사로 간주하는지
- 등장 가능한 NPC 목록
- 분위기/톤/잔혹도 가이드
- 한국어 응답 명시, 정형 질문 회피
- 에피소드 가이드를 따르되 사용자 입력을 우선해 가지치기하라는 안내

---

## 7. 본문(opening) 작성 규칙

`book.opening` 은 세션 시작 시 가장 먼저 채팅 영역에 표시되는 도입부 텍스트입니다. 메시지 히스토리에도 첫 user 엔트리로 들어가 LLM 컨텍스트에 항상 보존됩니다.

- 문장 단위로 `\n` 줄바꿈, 단락 전환은 `\n\n` 빈 줄.
- 한 채팅 버블 안에 모두 들어가므로 다섯 문장 이상이면 단락을 나누어 줄 것.
- 주인공의 시점을 명시 ("당신, 시로는…").
- 마지막 문장은 사용자가 첫 행동을 떠올리기 쉽도록 NPC 의 짧은 행동/대사 등 자극을 남겨 둘 것.

---

## 8. 입력 모드 (PlayScene)

`src/app/documents/playdocument.js` 의 입력창은 세 가지 모드를 가집니다. 모드는 사용자 입력을 LLM 에 보내기 전에 가공하는 방식을 결정합니다.

| 모드 | 가공 | 용도 |
| ---- | ---- | ---- |
| `dialogue` | 입력을 `"..."` 로 감싸 전송 | 순수 대사만 칠 때 |
| `mixed` | 입력 원문 그대로 전송 | 대사 + 괄호 안 상황 묘사 혼합 |
| `description` | 입력 원문 그대로 전송 | 행동/상황 묘사만 |
| (자동 진행) | 빈 입력일 때 `(자동 진행)` 으로 대체 | LLM 이 자율적으로 다음 장면 전개 |

`dialogue` 모드의 따옴표 감싸기 덕분에 LLM 측에서도 "이 입력은 토씨 그대로 주인공이 말한 대사" 임을 명확히 식별할 수 있습니다(사용자 입력 보존 지침의 큰따옴표 규칙과 연동).

---

## 9. 세션 영속화

`PlaybookSession.getSessionState()` 가 반환하는 객체가 `localStorage` 의 `playbook.slot.<slotId>` 키에 저장됩니다.

저장되는 항목:
- `messageHistory`: 전체 대화 기록 (역할/텍스트/모드)
- `currentEpisodeId`: 현재 진행 중인 화 ID
- `visitedEpisodeIds`: 방문한 화 ID 배열

UI 의 슬롯 카드(`RoomListScene`) 와 PlayScene 헤더는 `messageHistory` 의 `model` 응답 수를 세어 "N턴 진행" 으로 표시합니다.

---

## 동기화 메모

이 문서는 다음 코드와 1:1 로 대응합니다. 코드의 지침 텍스트를 수정할 때 반드시 이 문서도 같이 갱신해 주세요.

- `libs/playbook-engine.js/src/playbooksession.js`
  - `buildSystemPrompt()` — 섹션 조립 순서
  - `buildOutputFormatGuideText()` — §2
  - `buildUserInputPreservationGuideText()` — §3
  - `buildNpcReactionGuideText()` — §4
  - `buildEpisodeGuideText()` — §5
  - `buildLlmContents()` — §1 의 메시지 히스토리 슬라이딩
- `assets/data/books.json` — §6, §7
- `src/app/documents/playdocument.js` — §8, §9
